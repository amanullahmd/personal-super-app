# This is a monorepo configuration
# Railway will create separate services for each workspace

[build]
builder = "nixpacks"

# Don't deploy from root - this is just for monorepo management
[deploy]
# No deployment configuration for root
